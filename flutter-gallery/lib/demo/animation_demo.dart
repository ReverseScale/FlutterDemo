import 'package:flutter/material.dart';

class AnimationDemo extends StatelessWidget {
  static const String routeName = '/animation'; // Used by the Gallery app.

  @override
  Widget build(BuildContext context) => Text(routeName);
}